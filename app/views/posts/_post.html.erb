<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
    <h4>Posted by <%= link_to post.user.UserName,user_path(post.user)%></h4>
  </h2>
  <h6><%= pluralize(post.views, "view")%></h6>
<div>
  <%= post.body %>
</div>
</div>
